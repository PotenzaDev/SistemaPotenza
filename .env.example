# Usado só pelo docker-compose.yml para criar o container Postgres (db:).
# Os mesmos valores devem ser repetidos em backend/.env (DB_DATABASE, DB_USERNAME, DB_PASSWORD),
# já que o Laravel se conecta ao Postgres via backend/.env, não via este arquivo.
DB_DATABASE=potenza
DB_USERNAME=potenza
DB_PASSWORD=
